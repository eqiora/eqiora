---
title: Equations and state
description: Simultaneous equations, explicit initial conditions and periodic activation.
editUrl: https://github.com/nkiyohara/eqiora/edit/main/docs/site/src/content/docs/reference/language/equations.mdx
---

import { Code } from '@astrojs/starlight/components';
import ExactSourceLink from '@components/site/ExactSourceLink.astro';
import clocked from './_examples/clocked.eqi?raw';

[Language overview](/reference/language/) · [Install this source revision](/get-started/#install-with-uv)

## Continuous equations

Use `relation name { left = right; }`, optionally `on support` and
`at activation` before the body. A relation can contain several ordered pairs of
left and right expressions; all its equations hold simultaneously. For example,
`voltage = resistance * current` expresses Ohm's law, not an assignment.

Both sides are checked before the compiler forms a typed residual. A bare exact
zero can take the other side's dimension after expression admission; multiplying
an invalid expression by zero does not make it admissible. A valid equation does
not by itself establish that a numerical backend can solve it.

## Inequalities and complementarity

Conditions remain distinct from Boolean expressions and from their numerical enforcement:

```eqi
relation contact {
  2[N/m] * gap - force = load;
  complementarity(0[m] <= gap, force >= 0[N]);
  inequality(gap <= 4[m]);
}
```

`inequality(a <= b)` and `inequality(b >= a)` retain the same non-strict ordering. Their
operands must be invariant real scalars with the same physical type and support.
`complementarity(first, second)` requires two explicit nonnegativity predicates; the retained
physical operands may have different dimensions, such as gap and force, but must share support.
A bare operand, reversed sign, strict comparison, complex value or unsupported inclusion is
rejected.

The current executable profile is a finite, nonspatial affine active set. A Plan must name each
constraint, give coherent-SI tolerances for its operands and bound complete branch enumeration.
Execution checks the original Model conditions again after the linear backend succeeds and
retains the measured operands and active/inactive state in the Result. It does not introduce an
implicit penalty, clipping or smoothing. Spatial contact, friction, nonlinear constraints and
other formulations remain separate capabilities.

`derivative(x)` denotes the time derivative of an eligible continuous state, dividing its dimension by
time. Spatial operators such as `grad`, `div`, `trace` and `normal` have separate
support rules; the [continuum examples](/reference/standard-packages/continuum/)
show their existing standard declarations without selecting a discretization.

## Initial values and restart

The [ODE lesson](/learn/mathematical-modeling/ordinary-differential-equations/)
uses `state x: 1; initial { x = 1; }` and the residual `derivative(x) + rate * x = 0`.
Its complete source-installed example exercises `State.initial(plan)` and a run.
The simultaneous initial equation belongs to the Model; a solver guess, tolerance or time step
does not change that initial condition.

Use a supported accepted State for continuation rather than constructing a fresh
initial State at every output time. An initial value is a mathematical input, not an implicit result of choosing a solver.

## Periodic state

<Code code={clocked} lang="eqiora" title="clocked.eqi — compilation example" />

The clock specifies a period of exactly 1/10 second and a phase of zero.
`pre(memory)` refers to the pre-tick value; `next(memory)` participates in the
candidate update. `relation advance at tick` activates the equation on that clock. `pre` and `next`
require a state owned by that exact periodic clock; `derivative` is rejected there. This compilation
example does not select a Python periodic execution method.

<ExactSourceLink kind="blob" path="docs/site/src/content/docs/reference/language/_examples/clocked.eqi">Clocked example source</ExactSourceLink>
· <ExactSourceLink kind="blob" path="crates/eqiora-lang/src/parser/relation.rs">relation parser</ExactSourceLink>
· Next: [Composition](/reference/language/composition/)
