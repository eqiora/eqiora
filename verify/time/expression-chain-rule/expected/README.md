# Expected values

At the declared nonzero State rates, the polynomial, composed, fixed-Parameter and
explicit-time expressions equal their independent analytic right-hand sides exactly.
The generated and explicitly authored storage trajectories agree at t=0.25, 0.5 and
1 second; each differs from sqrt(1+2t) by less than 0.001. Initial state and rate
are 1 and 1 per second. Model replay retains the exact implicit-lowering proof.
